node_modules/
dist/
*.db
*.sqlite
.env
.env.*
!.env.example
server/certs/
server/public/
git-repos/
projects/
.DS_Store
*.pdf

# Personal LaTeX manuscripts kept in the repo root for convenience —
# not part of the FlowTex codebase.
/*.docx
/Manuscript*.tex
/main.tex
/-*.tex
/sorted_main.bib
/emse_official.cls
/spbasic.bst
/svglov3.clo
/svjour3.cls
/latexmkrc

# R session history (created by RStudio when opened in this dir)
.Rhistory

# Local user-space deploy scaffolding (machine-specific paths + runtime logs).
# Portable deployment guidance lives in docs/installation.html.
deploy/

# Playwright artefacts
test-results/
playwright-report/

# Stryker mutation-testing artefacts
.stryker-tmp/
server/reports/mutation/

# Local cache from the Understand-Anything plugin (knowledge graph,
# fingerprints, file-analysis artefacts). Regenerable; not source.
.understand-anything/

# Auto-generated docs (rebuilt by client/scripts/build-docs.mjs on
# every `npm run build`). Source of truth is the *.md files at the
# repo root.
client/public/docs/user-guide.html
client/public/docs/helper-guide.html
